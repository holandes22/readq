{{#if hasBlock}}
  {{yield (hash
      input=(component "one-way-input" value=(get changeset propertyName) update=(action "update"))
      error=(component "validated-input/error" changeset=changeset propertyName=propertyName)
    )}}
{{else}}
  {{one-way-input value=(get changeset propertyName) update=(action "update")}}
  {{validated-input/error changeset=changeset propertyName=propertyName}}
{{/if}}
