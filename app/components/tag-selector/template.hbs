{{#power-select-multiple
  options=tags
  loadingMessage="Fetching tags...."
  selected=selection.filterTags
  onchange=(action (mut selection.filterTags))
  as |tag|
}}
  {{tag}}
{{/power-select-multiple}}
